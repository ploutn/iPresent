import type { Plugin } from '..';
export interface AliasNode {
    type: 'require' | 'import';
    start: number;
    end: number;
    importee: {
        start: number;
        end: number;
        raw: string;
    };
}
export declare function alias(aliases: {
    find: string | RegExp;
    replacement: string;
}[], options?: {
    acornOptions?: Partial<import('acorn').Options>;
}): Plugin;
