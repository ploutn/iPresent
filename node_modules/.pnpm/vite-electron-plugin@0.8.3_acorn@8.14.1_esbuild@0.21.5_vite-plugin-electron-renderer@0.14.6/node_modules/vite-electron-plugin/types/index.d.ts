import type { Plugin as VitePlugin } from 'vite';
import { startup } from 'vite-plugin-electron';
import { type Configuration, type ResolvedConfig, type Plugin } from './config';
export { type Configuration, type ResolvedConfig, type Plugin, build, watch, startup, defineConfig, electron as default, };
declare function defineConfig(config: Configuration): Configuration;
declare function build(config: Configuration): Promise<{
    filename: string;
    code: string;
    map?: string | undefined;
    destname?: string | undefined;
}[]>;
declare function watch(config: Configuration): Promise<import("notbundle/types").FSWatcher>;
declare function electron(config: Configuration): VitePlugin[];
