import { type Configuration, type ResolvedConfig, type Plugin } from './config';
export type BuildResult = Parameters<NonNullable<Plugin['ondone']>>[0];
export declare function build(config: Configuration): Promise<BuildResult[]>;
export declare function buildFile(config: ResolvedConfig, filename: string): Promise<BuildResult>;
